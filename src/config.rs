/// Keeps track of the configurations of a gitrs project
#[derive(Debug)]
pub struct GitrsConfig {}

impl GitrsConfig {
    /// Flushes a GitrsConfig to the config file (check constants.rs for config path)
    pub fn to_file() {
        todo!()
    }

    /// Read a GitrsConfig from the config file
    pub fn from_file() -> Option<Self> {
        todo!()
    }
}
